import 'package:flutter/material.dart';

class InstructionsBody extends StatelessWidget {
  const InstructionsBody({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(child: Center(
      child: Text("Coming Soon..."),
    ));
  }
}
